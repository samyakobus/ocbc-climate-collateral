node_modules/
.next/
out/
build/
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.env
.env*.local
.vercel
*.tsbuildinfo
next-env.d.ts
coverage/
test-results/
playwright-report/
blob-report/
.playwright/
__pycache__/
*.py[cod]
.venv/
venv/

# Raw downloads (STORM basins, LHASA, DEM). Only the 200 sampled values are committed.
data/raw/
# Local PGlite data directory.
data/pglite/
# Downloaded go-pmtiles binary.
.cache/

# Fetched basemap archives are large; the z0-z6 floor is committed (plan 4.7).
public/basemap/*.pmtiles
!public/basemap/asia-z0-z6.pmtiles

# OMC ephemeral session state (keep the team task list)
.omc/state/*
!.omc/state/team/

# scratch captures at repo root
/ai-popup.png
/ai-dashboard.png
/aicheck.mts
